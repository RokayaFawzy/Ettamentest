import 'package:flutter/material.dart';

class DrCard extends StatelessWidget {
  const DrCard({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector(

      
    );
  }
}